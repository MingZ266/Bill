<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- 所使用的圆角的半径. -->
    <dimen name="cornerRadius">12dp</dimen>
    <!-- 小尺寸图标的边长. -->
    <dimen name="iconSmall">20dp</dimen>
    <!-- 中等尺寸图标的边长. -->
    <dimen name="iconMedium">30dp</dimen>
    <!-- 大尺寸图标的边长. -->
    <dimen name="iconLarge">50dp</dimen>
    <!-- 小号文本，用于小标题的文本尺寸. -->
    <dimen name="textSmall">14sp</dimen>
    <!-- 中号文本，用于显示多数文本的大小. -->
    <dimen name="textMedium">18sp</dimen>
    <!-- 比中号大但比大号小，用于在中号文本中突显的文本. -->
    <dimen name="textAboveM">20sp</dimen>
    <!-- 大号文本，用于大标题的尺寸. -->
    <dimen name="textLarge">22sp</dimen>
    <!-- 描边宽度. -->
    <dimen name="strokeWidth">1dp</dimen>
</resources>